import React, { useState } from 'react'
import { sidebarLinks } from '../constants'

const Sidebar = () => {
    const [open,setOpen]=useState();
  return (
    <div className={`h-screen bg-[#1e2640] w-72`}>

    </div>
  )
}

export default Sidebar